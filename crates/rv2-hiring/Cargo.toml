[package]
name = "rv2-hiring"
description = "UK tech hiring register: records, role classification and the Litmus filter model. No I/O, no clock. WASM-safe (layer 1)."
edition.workspace = true
license.workspace = true
version.workspace = true
# Never published: internal workspace crate. Also what makes cargo-deny's
# `allow-wildcard-paths` apply — it is skipped for publishable crates.
publish = false

[dependencies]
serde.workspace = true
serde_json.workspace = true
