[package]
name = "rv2-shared"
description = "Server-side cross-cutting types: ServerConfig, AuthUser, Role. SERVER-ONLY (layer 2)."
edition.workspace = true
license.workspace = true
version.workspace = true
# Never published: internal workspace crate. Also what makes cargo-deny's
# `allow-wildcard-paths` apply — it is skipped for publishable crates.
publish = false

[dependencies]
thiserror.workspace = true
uuid = { workspace = true, features = ["v4"] }
